import React from 'react';

const Intro = (props) => {
    return(
        <div className="intro">
            Working at intersection of design and technology. Experienced in cross-functional profiles across the domains of product design and frontend development.
        </div>
    );
};

export default Intro;
